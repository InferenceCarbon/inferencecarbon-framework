# Briefs

One subdirectory per published brief, each holding the brief's data folder
and its reproduce script. Tokenizer basis per brief is stated in the brief
itself; what each basis means is stated once, in `../calibration/README.md`.

<!-- TODO(source needed): populate gemini/ (o200k_base basis; its anchor
model was retired from the API on 1 June 2026 — the manifest entry in
../manifests/google.json is what makes that explanation reconstructable) and
add further briefs (Claude, on the calibrated-tokenizer basis) as they are
published. -->
